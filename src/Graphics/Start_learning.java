package Graphics;

import javax.swing.*;
import java.awt.*;
import java.awt.event.ActionEvent;
import java.sql.SQLException;

/* public class Start_learning {
    public void create_frame() {
        JFrame frame = new JFrame("Start");
        frame.setSize(1000, 1000);
        frame.getContentPane().setBackground(new Color(0xFDE335));
        JButton button_START = new JButton("START");
        frame.add(button_START);
        button_START.setBounds(400, 400, 200, 100);
        button_START.setBackground(new Color(0x740572));

        button_START.addActionListener(new AbstractAction() {
            @Override
            public void actionPerformed(ActionEvent e) {
                try {
                    new home_frame().create();
                } catch (SQLException throwables) {
                    throwables.printStackTrace();
                } catch (ClassNotFoundException classNotFoundException) {
                    classNotFoundException.printStackTrace();
                }
            }
        });
        frame.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        frame.setLayout(null);
        frame.setVisible(true);
    }

}

 */
