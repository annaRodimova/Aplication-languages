package Graphics;

import javax.swing.*;
/* import java.awt.*;

public class Buttons_add_word {
    public void create_buttons() {
        JFrame frame_add = new JFrame("Start");
        frame_add.setSize(1000, 1000);
        frame_add.getContentPane().setBackground(new Color(0xFDE335));
        frame_add.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        frame_add.setLayout(null);
        frame_add.setVisible(true);
        Add_word word = new Add_word();
        for (int i = 0; i< word.return_word().size(); i ++) {
            frame_add.add(word.return_word().get(i));
        }
    }
}

 */
